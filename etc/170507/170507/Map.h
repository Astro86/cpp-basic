
#pragma once

#include "value.h"

// 헤더에 함수 선언을 만들고 Ctrl + . 을 누른 후에 Create Dclare/Definition을
// 누르면 cpp에 함수가 자동으로 만들어진다.
bool InitMap(PMONSTER pMonster);
void RunMap(PPLAYER pPlayer, PMONSTER pMonsterPrototype);
